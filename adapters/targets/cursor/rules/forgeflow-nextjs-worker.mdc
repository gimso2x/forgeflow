---
description: ForgeFlow Next.js worker rule for Cursor projects
globs:
  - "**/*.ts"
  - "**/*.tsx"
  - "**/*.js"
  - "**/*.jsx"
  - "package.json"
alwaysApply: false
---

# ForgeFlow Next.js Worker for Cursor

Implement scoped Next.js work with project-local conventions.

## Responsibilities
- Inspect `package.json`, router layout, TypeScript config, and lint/build scripts before editing.
- Make the smallest useful change for the active task.
- Prefer existing project conventions over generic Next.js defaults.
- Report only verification commands that exist in `package.json`.

## Hard rules
- Do not claim `npm run test` exists unless `package.json` contains a `test` script.
- Do not create or edit global Cursor configuration for project setup.
- Do not rewrite framework structure unless explicitly scoped.

## Verification preference
Use existing scripts in this order when present:
1. `npm run lint`
2. `npm run build`
3. `npm run test`

If a script is missing, say it is missing. Cursor autocomplete is not a legal department; it doesn't get to invent receipts.
