{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "RunState",
  "type": "object",
  "required": [
    "schema_version",
    "task_id",
    "current_stage",
    "status",
    "completed_gates",
    "failed_gates",
    "retries",
    "spec_review_approved",
    "quality_review_approved"
  ],
  "properties": {
    "schema_version": {"type": "string"},
    "task_id": {"type": "string"},
    "current_stage": {"enum": ["clarify", "plan", "execute", "spec-review", "quality-review", "finalize", "long-run"]},
    "status": {"enum": ["not_started", "in_progress", "blocked", "completed", "failed"]},
    "completed_gates": {
      "type": "array",
      "items": {"enum": ["clarification_complete", "plan_executable", "execution_evidenced", "spec_review_passed", "quality_review_passed", "ready_to_finalize", "worth_long_run_capture"]}
    },
    "failed_gates": {
      "type": "array",
      "items": {"enum": ["clarification_complete", "plan_executable", "execution_evidenced", "spec_review_passed", "quality_review_passed", "ready_to_finalize", "worth_long_run_capture"]}
    },
    "retries": {
      "type": "object",
      "propertyNames": {"enum": ["clarify", "plan", "execute", "spec-review", "quality-review", "finalize", "long-run"]},
      "additionalProperties": {"type": "integer", "minimum": 0}
    },
    "evidence_refs": {"type": "array", "items": {"type": "string"}},
    "spec_review_approved": {"type": "boolean"},
    "quality_review_approved": {"type": "boolean"},
    "final_status": {"enum": ["success", "partial", "failed", "cancelled"]}
  },
  "additionalProperties": false
}